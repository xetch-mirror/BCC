# GCC Test Program

`test_program.c` is a small GCC link-and-run check.

It prints the integer `42`, then verifies that the value is the expected integer:

- Success: `yeah, tests done`
- Failure: `TEST GONE WRONG!`

Run it with:

```sh
make test
```
